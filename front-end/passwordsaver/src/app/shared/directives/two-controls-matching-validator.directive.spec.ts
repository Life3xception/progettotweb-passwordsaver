import { TwoControlsMatchingValidatorDirective } from './two-controls-matching-validator.directive';

describe('TwoControlsMatchingValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TwoControlsMatchingValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
